import '../item.dart';
import '../user.dart';

class Task {
 /* DateTime dueDate;
  List<User> membersAssigned;   //Whos responsibility is it?
  bool isReminder;

  Task(description, author, this.dueDate,
      {
        List<User>? membersAssigned,  //Nullable
        bool? isReminder
      }) :
        membersAssigned = membersAssigned ?? [],
        isReminder = false,
        super(description, author);

  DateTime getDueDate(){
    return dueDate;
  }
  bool getIsAssigned(){
    return membersAssigned.isEmpty;
  }
  List<User> getMembersAssigned(){
    return membersAssigned;
  }*/
}